#include <stdio.h>

int main() {
    int n1, n2;

    // Input two numbers
    printf("Enter two numbers: ");
    scanf("%d %d", &n1, &n2);

    // Print with space in between
    printf("%d %d\n", n1, n2);

    return 0;
}
